package gui.distancia_minima;

import java.awt.event.KeyAdapter;
import java.awt.event.KeyEvent;

public class DistanciaMinimaController {
	
	
	public void listenerIframe(DistanciaMinimaContainer distanciaMinimaGui){
		
	/*	distanciaMinimaGui.txtX1.addKeyListener(new KeyAdapter(){
            public void keyTyped(KeyEvent e){
                   char caracter = e.getKeyChar();
                   if(((caracter < '0') || (caracter > '9')) && (caracter != KeyEvent.VK_BACK_SPACE) && (caracter != '.') && (caracter !='-'))
                	   e.consume();              
                }
         });
		
		
		distanciaMinimaGui.txtPc.addKeyListener(new KeyAdapter(){
            public void keyTyped(KeyEvent e){
                   char caracter = e.getKeyChar();
                   if(((caracter < '0') || (caracter > '9')) && (caracter != KeyEvent.VK_BACK_SPACE) && (caracter != '.') && (caracter !='-'))
                	   e.consume();              
                }
         });
		
		distanciaMinimaGui.txtPm.addKeyListener(new KeyAdapter(){
            public void keyTyped(KeyEvent e){
                   char caracter = e.getKeyChar();
                   if(((caracter < '0') || (caracter > '9')) && (caracter != KeyEvent.VK_BACK_SPACE) && (caracter != '.') && (caracter !='-'))
                	   e.consume();              
                }
         });
		*/
	}

}
